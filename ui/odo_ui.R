tabPanel("One-Day Operations",
           fluidRow(
             align = "center",
             plotOutput("odo", height = plot.height, width = plot.width)
           ) # End fluidRow
) # End tabPanel